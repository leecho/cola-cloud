server:
  context-path: /uaa
  port: 5000

management:
  security:
    enabled: false

security:
  oauth2:
    resource:
      id: auth-service

cola:
  wechat:
    miniapp:
      appid: ${COLA_WECHAT_MINIAPP_APPID}
      secret: ${COLA_WECHAT_MINIAPP_SECRET}